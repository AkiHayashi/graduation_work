
<%= simple_form_for(@health_status) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :systolic %>
    <%= f.input :diastolic %>
    <%= f.input :pulse %>
    <%= f.input :condition %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
