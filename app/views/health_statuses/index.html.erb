<h1>健康状態の記録</h1>

<h3>収縮期血圧（上）</h3>
<%= line_chart @systolic, colors: ["#b00", "#666"], xtitle: "日付", ytitle: "測定値", empty: "データがありません", discrete: true %>


<h3>拡張期血圧（下）</h3>
<%= line_chart @diastolic, xtitle: "日付", ytitle: "測定値", empty: "データがありません", discrete: true%>

<h3>脈拍</h3>
<%= line_chart @pulse, colors: ["#666"], xtitle: "日付", ytitle: "測定値", empty: "データがありません", discrete: true%>

<table>
  <thead>
    <tr>
      <th>収縮期血圧（上）</th>
      <th>拡張期血圧（下）</th>
      <th>脈拍</th>
      <th>体調</th>
      <th>測定日</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @health_statuses.each do |health_status| %>
      <tr>
        <td><%= health_status.systolic %></td>
        <td><%= health_status.diastolic %></td>
        <td><%= health_status.pulse %></td>
        <td><%= health_status.condition %></td>
        <td><%= l health_status.created_at, format: :long %></td>
        <td><%= link_to 'この記録の詳細を見る', health_status %></td>
        <td><%= link_to 'この記録を編集する', edit_health_status_path(health_status) %></td>
        <td><%= link_to '削除', health_status, method: :delete, data: { confirm: '本当に削除してもいいですか?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '健康状態を入力する', new_health_status_path %>
