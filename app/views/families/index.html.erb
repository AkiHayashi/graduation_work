<h1>わたしの家族</h1>

<table>
  <thead>
    <tr>
      <th>家族名</th>
      <th>家族の写真</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% current_user.families.each do |family| %>
      <tr>
        <td><%= family.name %><p>家</p></td>
        <td><%= image_tag family.image.url, size: '400x300'%></td>
        <td><div class="btn btn-border"><%= link_to 'この家族のページへ', family %></td></div>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<div class="btn btn-border">
<%= link_to '新しい家族を作成する', new_family_path %></div>
